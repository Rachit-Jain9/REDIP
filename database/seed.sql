-- REDIP intentionally ships without demo business data.
-- Register a real user and add verified pipeline records through the app.

SELECT 'REDIP seed completed: no mock or demo records inserted.' AS message;
